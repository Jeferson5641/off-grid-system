# 🌐 Guia de Internacionalização (i18n)

Este documento descreve como utilizar o sistema de internacionalização (i18n) da aplicação Off-Grid System Calculator.

## 📋 Estrutura Atual

### Arquivos de Configuração

- **`src/lib/i18n.ts`** - Arquivo principal de configuração de idiomas

  - Define os idiomas suportados
  - Contém todas as traduções
  - Fornece função auxiliar `getTranslation()`

- **`src/components/LanguageSwitcher.tsx`** - Componente de seleção de idioma
  - Menu dropdown na parte superior direita
  - Salva a preferência no `localStorage`
  - Links para README em diferentes idiomas

### Idiomas Suportados

- 🇧🇷 **Português (Brasil)** - `pt-BR`
- 🇺🇸 **English (USA)** - `en-US`

## 🚀 Como Usar

### 1. Adicionar Novas Traduções

Edite o arquivo `src/lib/i18n.ts` e adicione novas chaves no objeto `translations`:

```typescript
export const translations = {
  "pt-BR": {
    novaSecao: {
      chave1: "Valor em Português",
      chave2: "Outro valor",
    },
  },
  "en-US": {
    novaSecao: {
      chave1: "Value in English",
      chave2: "Another value",
    },
  },
};
```

### 2. Usar a Função de Tradução

```typescript
import { getTranslation, Language } from "@/lib/i18n";

const language: Language = "pt-BR";
const texto = getTranslation(language, "novaSecao.chave1");
// Resultado: "Valor em Português"
```

### 3. Acessar Estado do Idioma Atual

```typescript
"use client";

import { useState } from "react";
import { Language, translations } from "@/lib/i18n";

export default function MeuComponente() {
  const [language, setLanguage] = useState<Language>("pt-BR");

  const t = translations[language];

  return (
    <div>
      <h1>{t.novaSecao.chave1}</h1>
    </div>
  );
}
```

## 🔄 Fluxo de Seleção de Idioma

```
┌─────────────────────────────────────┐
│  Usuário clica no menu de idioma    │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│  Seleciona novo idioma              │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│  Salva preferência no localStorage  │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│  Recarrega a página                 │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│  Carrega idioma salvo               │
└─────────────────────────────────────┘
```

## 📝 Estrutura de Traduções Recomendada

```typescript
const translations = {
  "pt-BR": {
    menu: {
      /* Traduções do menu */
    },
    solar: {
      /* Traduções da calculadora */
    },
    errors: {
      /* Mensagens de erro */
    },
    success: {
      /* Mensagens de sucesso */
    },
    labels: {
      /* Rótulos de campos */
    },
  },
};
```

## 🔗 Links do README

Os links para README diferentes idiomas:

- **Português**: `https://github.com/Jeferson5641/off-grid-system/blob/master/README.md`
- **English**: `https://github.com/Jeferson5641/off-grid-system/blob/master/README.en.md`

## 🎨 Menu de Idioma

O componente `LanguageSwitcher` fornece:

1. **Seletor de Idioma** - Muda o idioma da aplicação
2. **Links para README** - Abre documentação em diferentes idiomas
3. **Persistência** - Salva preferência do usuário
4. **UI Responsiva** - Adapta-se a diferentes tamanhos de tela

### Posicionamento

- **Localização**: Canto superior direito (fixed)
- **Z-index**: 50 (fica sobre outros elementos)
- **Classes Tailwind**: `fixed top-6 right-6 z-50`

## ⚙️ Configuração Futura

Para implementação completa de i18n com Next.js 13+, considere usar:

1. **next-intl** - Suporte robusto a internacionalização
2. **i18next** - Gerenciador de traduções flexível
3. **react-i18next** - Integração com React

### Exemplo com next-intl

```typescript
// Configuração futura
import { useTranslations } from "next-intl";

export default function Component() {
  const t = useTranslations();
  return <h1>{t("titulo")}</h1>;
}
```

## 🧪 Testando Traduções

```typescript
import { getTranslation } from "@/lib/i18n";

// Teste
console.log(getTranslation("pt-BR", "solar.title"));
// Resultado: "☀️ Calculadora Off-Grid Solar"

console.log(getTranslation("en-US", "solar.title"));
// Resultado: "☀️ Off-Grid Solar Calculator"
```

## 📦 Adicionando Novo Idioma

1. Adicione a nova linguagem em `src/lib/i18n.ts`:

```typescript
export type Language = "pt-BR" | "en-US" | "es-ES"; // Novo idioma

export const languages: Record<Language, { name: string; flag: string }> = {
  "pt-BR": { name: "Português", flag: "🇧🇷" },
  "en-US": { name: "English", flag: "🇺🇸" },
  "es-ES": { name: "Español", flag: "🇪🇸" }, // Novo
};

export const translations = {
  "pt-BR": {
    /* ... */
  },
  "en-US": {
    /* ... */
  },
  "es-ES": {
    /* Novas traduções */
  },
};
```

2. Adicione o README correspondente:

   - `README.es.md`

3. Atualize o mapeamento de links em `LanguageSwitcher.tsx`:

```typescript
const readmeUrls: Record<Language, string> = {
  "pt-BR": "...",
  "en-US": "...",
  "es-ES": "...", // Novo
};
```

## 💡 Boas Práticas

✅ **Use chaves significativas** - Exemplos: `solar.title`, não `t1`, `t2`  
✅ **Organize por seções** - Agrupe traduções relacionadas  
✅ **Mantenha consistência** - Use os mesmos termos em todas as traduções  
✅ **Documente contexto** - Adicione comentários para traduções ambíguas  
✅ **Teste em múltiplos idiomas** - Verifique layout e comprimento de texto

## ❌ Armadilhas Comuns

❌ **Não copie strings hardcoded** - Sempre use o sistema de traduções  
❌ **Não misture idiomas** - Mantenha separação clara entre PT e EN  
❌ **Não ignore localStorage** - Persista preferências do usuário  
❌ **Não esqueça README** - Mantenha documentação atualizada em todos os idiomas

## 📞 Suporte

Para dúvidas sobre internacionalização ou sugestões de melhorias, abra uma issue no GitHub:

https://github.com/Jeferson5641/off-grid-system/issues

---

**Versão do Guia**: 1.0  
**Última Atualização**: 14 de Novembro de 2025
